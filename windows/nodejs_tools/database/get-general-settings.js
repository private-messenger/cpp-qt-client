const sql = require('sql-easy-lib');

/*
Порядок следования:
<maxAddressBook>
<maxPeronalAddress>
*/

function getGeneralSettingsFunct (database) {
	let generalSettingsData = "";
	
	process.stdout.write(generalSettingsData);
}